export const products = (state) => {
    return state.products
}

export const fields = (state) => {
    return state.fields
}

export const meta = (state) => {
    return state.meta
}

export const links = (state) => {
    return state.links
}

export const activeProduct = (state) => {
    return state.activeProduct
}

export const productInventory = (state) => {
    return state.productInventory
}

export const deleted = (state) => {
    return state.deleted
}